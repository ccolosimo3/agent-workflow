interface:
  display_name: "Independent Review"
  short_description: "Run the independent outer implementation gate"
  default_prompt: "Use $independent-review to review this converged implementation independently."
policy:
  allow_implicit_invocation: false
