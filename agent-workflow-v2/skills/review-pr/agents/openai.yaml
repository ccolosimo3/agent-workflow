interface:
  display_name: "Review PR"
  short_description: "Run a thorough calibrated coworker PR review"
  default_prompt: "Use $review-pr to review this coworker's pull request."
policy:
  allow_implicit_invocation: false
