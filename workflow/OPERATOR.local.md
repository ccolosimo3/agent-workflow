# Operator identity & context

Local-only, git-excluded. Read by agents in every repo via the kernel's Startup
Adapters pointer. One person, multiple handles — the links below are not
derivable from any single source, so they are written down here.

- **Name:** Chris Colosimo.
- **Linear:** displayName `ccolosimo` · `ccolosimo@enbasis.com` · user id
  `b268963f-86f5-4095-9e01-bf1c21e42228`. Teams: Engineering (`EGN`, primary for
  code work), Commerce (`COM`), Web (`WEB`), Vendors, Business (`BIZ`),
  S4G Backend (`S4G`).
- **GitHub:** login `ccolosimo3` (renamed from `odysian` — older PRs/commits and
  any lingering `odysian` references are the same person).
- **git commit identity:** `user.name = ccolosimo3`, `user.email = odysian7@gmail.com`.

To list the operator's Linear issues, filter assignee "me" (or "Chris Colosimo" /
the user id above). To find their GitHub PRs/commits, use login `ccolosimo3` (add
`odysian` when searching older history). The `ccolosimo3` ↔ `ccolosimo` ↔ Chris
Colosimo link is not derivable from any single source.
