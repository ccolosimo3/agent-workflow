--- workflow/ORCHESTRATOR_MODE.md.before
+++ workflow/ORCHESTRATOR_MODE.md.after
@@ -2,2 +2,2 @@
-title: Orchestrator Mode Draft
-status: draft-v0
+title: Orchestrator Mode V0
+status: active-v0
@@ -16 +16 @@
-# Orchestrator Mode Draft
+# Orchestrator Mode V0
@@ -18,3 +18,4 @@
-This is a candidate kernel mode, not active policy. Until it is reviewed and
-ratified, agents continue to use Startup Routing A-D in `AGENTS.md` and the
-existing planning, execution, and review contracts.
+This is the active V0 kernel mode for explicitly authorized end-to-end program
+orchestration. Startup Routing E in `AGENTS.md` and the `/orchestrate` skill
+enter this mode; existing planning, execution, and review contracts remain in
+force beneath it.
@@ -33 +34 @@
-The eventual product shape should be both:
+The product shape is both:
@@ -273 +274 @@
-## Candidate Invocation Shape
+## Invocation Shape
@@ -275,2 +276 @@
-Do not make this a canonical kickoff until the mode is reviewed. A future
-`/orchestrate` invocation could begin from:
+An explicit `/orchestrate` invocation begins from:
@@ -301 +301 @@
-## Promotion Questions
+## Post-V0 Follow-ups
@@ -303 +303,3 @@
-Before this becomes policy, resolve:
+The reviewed V0 resolved the trigger, ledger, desktop-task control, serial-writer
+topology, model routing, review, recovery, and pilot questions. Continue to
+investigate these non-blocking follow-ups without silently expanding V0:
@@ -305,14 +307,4 @@
-1. Exact `/orchestrate` trigger phrases and whether a tracker-backed spec is
-   mandatory.
-2. Machine-readable ledger schema and where status is rendered.
-3. Host-specific fresh desktop-task creation, explicit model/reasoning routing,
-   monitoring, continuation, replacement, and archival behavior.
-4. Default concurrency ceiling and criteria for raising it.
-5. Whether ordinary scoped commits are included by default or named per program.
-6. How an approved action and its remaining cap are propagated without creating
-   a reusable authority token.
-7. Heartbeat/stall thresholds and when replacement is allowed.
-8. Integration-owner review independence when the orchestrator performs a
-   mechanical merge or patch.
-9. Command-rule/hook enforcement and worktree readiness prerequisites.
-10. A small pilot program and success metrics before broad rollout.
+1. Prove worktree readiness before enabling parallel implementation.
+2. Evaluate command-rule/hook enforcement only through its separate threat model.
+3. Calibrate heartbeat/stall thresholds from additional real programs.
+4. Revisit the one-writer and read-only ceilings only with measured evidence.
@@ -320 +312 @@
-## Suggested Rollout
+## V0 Rollout Record
@@ -322,9 +314,4 @@
-1. Review this mode against the existing kernel and first experiment notes.
-2. Test it manually on one serial, offline, two-task program.
-3. Add the structured ledger/status templates and a non-canonical kickoff draft.
-4. Prove fresh desktop-task control in the V0 pilot; prove worktree readiness
-   separately before enabling parallel implementation.
-5. Add command guardrails and verification hooks after their dedicated threat-
-   model/spec pass.
-6. Promote the mode and `/orchestrate` skill only after the pilot retrospective
-   shows fewer pauses and environment repairs without weaker review or safety.
+V0 was reviewed against the kernel, exercised through a serial offline pilot and
+real desktop-task control pilot, patched through inner and outer review, and
+promoted only through a manifest-verified activation candidate. Worktree
+parallelism and command hooks remain outside V0.
