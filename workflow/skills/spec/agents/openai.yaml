interface:
  display_name: "Spec"
  short_description: "Write a right-sized implementation-ready specification"
  default_prompt: "Use $spec to turn this work item into a grounded, review-ready implementation spec."
policy:
  allow_implicit_invocation: true
