interface:
  display_name: "Spec"
  short_description: "Formally plan one selected work item"
  default_prompt: "Use $spec to produce an implementation-ready plan for this issue."
policy:
  allow_implicit_invocation: false
