interface:
  display_name: "Project Lead"
  short_description: "Coordinate scoped project planning and execution"
  default_prompt: "Use $project-lead to get current, shape the next work, and coordinate execution without implementing it here."
policy:
  allow_implicit_invocation: false
