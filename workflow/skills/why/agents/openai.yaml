interface:
  display_name: "Why It Exists"
  short_description: "Investigate rationale from code history and evidence"
  default_prompt: "Use $why to investigate why this code or policy exists from evidence and history."
policy:
  allow_implicit_invocation: true
