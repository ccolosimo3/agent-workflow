interface:
  display_name: "Blast Radius"
  short_description: "Find and prove non-obvious downstream change risk"
  default_prompt: "Use $blast-radius to determine what this change could break beyond the diff."
policy:
  allow_implicit_invocation: true
