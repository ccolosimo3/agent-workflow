interface:
  display_name: "Explore"
  short_description: "Compare architectural options with bounded evidence"
  default_prompt: "Use $explore to compare the credible approaches for this architectural question."
policy:
  allow_implicit_invocation: true
