interface:
  display_name: "Review Change"
  short_description: "Run one fresh inner implementation review"
  default_prompt: "Use $review-change to review this completed implementation."
policy:
  allow_implicit_invocation: false
