# Machine bootstrap kits

Bootstrap kits restore the portable workflow from the private repository without
copying machine-specific auth, secrets, caches, or runtime state.

- `windows-git-bash/` — native Windows wiring for Codex and Claude Code using
  Git Bash.
