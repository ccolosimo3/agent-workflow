# Merge these portable preferences into ~/.codex/config.toml.
# Do not overwrite the app-generated file.
# Approval and sandbox settings are intentionally omitted for the initial setup.

model = "gpt-5.6-sol"
model_reasoning_effort = "high"
service_tier = "default"

[desktop]
enabled-reasoning-efforts = ["low", "medium", "high", "xhigh", "ultra", "max"]
